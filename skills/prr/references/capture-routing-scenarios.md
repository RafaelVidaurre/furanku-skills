# Capture-routing regression scenarios

Use these scenarios to check the Capture step 1 boundary. The existing PRR state named in each scenario is authoritative.

## Corrective defect already entailed by accepted intent

Existing state: answered-detail screens present clear, structured content without repetition.

> This view needs work; replies repeat content and it is chaotic. Track it in Beads; do not coordinate work yet.

Expected: create the requested Beads bug and preserve the no-coordination scope. Create no PRR scratch candidate, derive no negative requirement, and present no PRR confirmation prompt. The accepted requirement already entails the desired result.

## Defect plus a separate new boundary

Existing state: the same accepted structured-content requirement.

> This view repeats content. Track the bug. From now on, answered-detail screens must put the final answer first and collapse supporting discussion by default.

Expected: route the repetition report as corrective work. Separately capture the two independently changeable enduring commitments—answer-first ordering and collapsed-by-default discussion—and present them for confirmation at a natural pause.

## Explicit replacement

Existing state: supporting discussion is collapsed by default.

> Actually, answered-detail screens should show the entire discussion expanded by default. Replace the collapsed-detail requirement.

Expected: capture this as a change attached to the current state and route the incompatible claims through Conflicts. Preserve and replace state only after final wording and status are confirmed.

## Unrelated new product intent

> Record this product direction: users must be able to export their account data.

Expected: capture a new requirement candidate immediately and ask for confirmation of synthesized wording and status at a natural pause.

## Self-contained source evidence

This scratch line fails Source entry:

> Source: current User conversation in the bluebird Agent Session.

The session label is an ephemeral locator and supplies no subject context. Store stable context such as `Context: defining the default presentation of answered-detail screens`, followed by the verbatim exchange. The copied exchange—not a session reference—is the evidence.
